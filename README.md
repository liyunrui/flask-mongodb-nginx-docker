# Blueprint for lask-mongodb-nginx-gunicorn-docker web application
Using docker to deploy a micro-service including web server, application server, and mongo database.

## Run

```bash
make build
```

To clean up the container mess, run
```
make clean
```
